package com.techlabs.test;

import java.util.Scanner;

import com.techlabs.exceptions.UserExceptions;
import com.techlabs.exceptions.UserExecptions;
import com.techlabs.model.User;

public class UserTest {

	public static void main(String[] args) {
		Scanner scanner = new Scanner(System.in);
		User user= new User();
		
		System.out.println("ENTER YOUR EMAIL : ");
		String email=scanner.next();
		emailReader(email,user);
		
		System.out.println("ENTER PASSWORD");
		String password=scanner.next();
		
		System.out.println(user);
		

	}
	
	private static void emailReader(String email, User user)
	{
		if(user.emailValidator(email)!=true)
		throw new UserExceptions();
		
		user.setEmail(email);	
	}
	
	private static void passwordlReader(String password, User user)
	{
		
	}
}
